% !TEX root = ../thesis.tex
\chapter{Formalizing the Problem} 
The existing research
on school choice and course allocation would seem to suggest that for our
problem, which only features one-sided preferences as opposed to preferences on
both sides of the market, we follow a fair division approach similar to that of
Budish’s one-sided matching \cite{budish}. However, given the practical application of our
problem as a mechanism that is meant to reduce the Registrar's workload, we
determined that constructing a notion of stability makes more sense than
evaluating envy in the matching. While we care about student happiness in our
solution, our formulation of the problem is primarily driven by the goal of
assigning year-long HUM sections that are balanced both in size and student
majors, meaning we want to minimize the desire of students to swap with each
other post-assignment so as not to disrupt the balance of students. If no student wishes to swap with
another student, then we can consider the assignment stable. This is the
framework under which we shall formalize our model.

\section{The Model}

The basic components of our model are the two sides of our market: a set of
students and a set of HUM class sections, each of which have a certain number of
seats based on the class capacity. Though we are working in a bipartite setting,
only one side of our market has preference over the other (i.e., students prefer
certain classes, whereas classes are inanimate and therefore indifferent to
students...as far as we know). Students
also do not necessarily have ranked preference over every single available
section; they likely equally do not want to take any sections that conflict with
their other selected courses. So, preference lists will be incomplete, with
those sections that students have a conflict with not being included in their ranking.
\newpage

\begin{definition} \label{def:overview}
  The \emph{HUM-assignment} problem consists of a finite set of Reed College
  students, $R$, and a finite set of class sections, $C$. Each class section $c
    \in C$ has a capacity $q_c$ representing the number of seats in the class,
    such that we can think of each $c$ as a set of seats where $|c| \leq q_c$.
    We denote the set of all seats $S$, where $c \subset S$ for all $c \in C$.
    Students rank class sections such that each student $r \in R$ has a strict
    preference relation $\succ_s$ over $C \cup \{\emptyset\}$, where $\emptyset$
    represents the outside option of a student such that $\emptyset \succ_s c$
    for some $c \in C$ denotes that the class section is not an option for the student.
\end{definition}

\begin{definition} \label{def:1m}
  A \emph{1-sided matching (1M)} of students to class sections is a function $\mu :
    R \rightarrow S \cup \emptyset $ such that
  \begin{itemize}
    \item
          Each student is assigned to a seat in a class section they prefer or no
          seat: \\ $\mu(r) =
            \emptyset \text{ or } \mu(r) \succ_r \emptyset \text{ for all }r \in
            R$.
    \item
          No class exceeds its capacity:
          $|\{r \in R : \mu(r) = s_c\}| \leq q_c$ \text{ for all } $s_c \in c
          \subset S$.
  \end{itemize}
\end{definition}

Finding a 1-sided matching would solve an instance of the HUM-assignment problem
as defined above. However, we still have to introduce the last constraint of the
problem: \emph{group fairness}.

Students can be divided into three groups based on the way Reed College divides
subjects: Group 1 for language, arts, and literature, Group 2 for
social sciences, Group 3 for math and lab sciences \cite{group-reqs}. To implement the HUM
philosophy, which is that the class provides an opportunity for students from all
knowledge backgrounds and academic interests to discuss relevant texts, we want
to ensure a distribution of students that "fairly" represents each group in every
HUM section. Practically, this means avoiding scenarios such as all Chem 101 students
with the same lab being placed
in the same section due to the overwhelming similarity in their schedules. This
will come in the form of \textit{quotas or caps} on the number of seats per section that
can be assigned to each group of student, so each class will be divided 
into four groups of available seats: A, B, C, and free seats to allow for some
flexibility.

\begin{definition} \label{def:student-groups}
  Under the group fairness constraint, $R$ is partitioned into
  three sets: $A, B, C \subset R$, where $A \cup B \cup C \subseteq R$ and $A \cap B \cap
    C = \emptyset$. Denote students in $A$ as $a_1, a_2, ..., a_i$, students in
    $B$ as $b_1, b_2, ..., b_j$, and students in $C$ as $c_1, c_2, ..., c_k$.
\end{definition}

\begin{definition} \label{def:group-caps}
  For each class $c \in C$ with capacity $q_c$, \[ |c| = q_c = \sum_{t \in
  \{A,B,C,F\}} q_c^t \] where $A, B, C, F \subseteq
c$. Similarly to Definition \ref{def:student-groups}, sets $A, B, C$ correspond to
the groups of students. $F$ represents the set of free seats.
\end{definition}

\begin{definition} \label{def:f1m}
  A \emph{group-fair 1-sided matching (F1M)} builds on Definition \ref{def:1m}
  of a 1M by adding the quality that no class is assigned more students of each
  group than there are reserved and free seats for those students, maintaining that no more
  total seats are matched for a class than its overall capacity:
  $|\{t \in T : \mu(t) = s_c^T\}| \leq q_c^T + q_c^F$ and $|\{r \in R : \mu(r) =
  s_c\}| \leq q_c$ \text{ for all } $s_c \in c \subset S$.
\end{definition}

To make things clearer, we can represent our problem as a graph to provide us
with a visualization of the problem (see Figure \ref{fig:model}).

\begin{figure}[H] \centering
\definecolor{plasmaA}{HTML}{0D0887}
\definecolor{plasmaB}{HTML}{CC4678}
\definecolor{plasmaC}{HTML}{F89441}
 \begin{tikzpicture}[
  classnode/.style={circle, draw, minimum size=1cm, inner sep=0},
  studentnode/.style={circle, draw, minimum size=0.8cm, inner sep=0, thick},
  ->, >=Stealth
]

%% ── Column headers ──────────────────────────────────────────────
\node at (-3, 1.8) {$\mathcal{C}$};
\node at (-3, 1.2) {\textsc{classes}};
\node at (-1.25, 1.8) {$\mathcal{S}$};
\node at (-1.25, 1.2) {\textsc{seats}};
\node at (3.5,  1.8) {$R$};
\node at (3.5,  1.2) {\textsc{students}};

%% ── Preference box (top right) ──────────────────────────────────
\node[draw, minimum width=2.8cm, minimum height=0.7cm, align=center]
(prefbox) at (7.5, 0.2) {$c_1 \succ_{a_1} c_3 \succ_{a_1} c_n$};

%% ── Helper macro: 4 coloured squares in a row ───────────────────
\newcommand{\seats}[2]{
  \draw[plasmaA, thick] (#1-0.45, #2-0.15) rectangle (#1-0.15, #2+0.15);
  \draw[plasmaB, thick] (#1-0.10, #2-0.15) rectangle (#1+0.20, #2+0.15);
  \draw[plasmaC, thick] (#1+0.25, #2-0.15) rectangle (#1+0.55, #2+0.15);
  \draw[black,   thick] (#1+0.60, #2-0.15) rectangle (#1+0.90, #2+0.15);
}

%% ── Class C1 ────────────────────────────────────────────────────
\node[classnode] (c1) at (-3, 0) {$c_1$};
\seats{-1.5}{ 0.45}
\seats{-1.5}{ 0.00}
\node at (-1.2, -0.38) {$\cdots$};
\seats{-1.5}{-0.75}

%% ── Class C2 ────────────────────────────────────────────────────
\node[classnode] (c2) at (-3, -2.8) {$c_2$};
\seats{-1.5}{-2.35}
\seats{-1.5}{-2.80}
\node at (-1.2, -3.18) {$\cdots$};
\seats{-1.5}{-3.55}

%% ── Vertical dots between C2 and Cn-1 ──────────────────────────
\node at (-3, -4.5) {$\vdots$};

%% ── Class Cn-1 ──────────────────────────────────────────────────
\node[classnode] (cn1) at (-3, -6.2) {$c_{n-1}$};
\seats{-1.5}{-5.75}
\seats{-1.5}{-6.20}
\node at (-1.2, -6.58) {$\cdots$};
\seats{-1.5}{-6.95}

%% ── Class Cn ────────────────────────────────────────────────────
\node[classnode] (cn) at (-3, -9.0) {$c_n$};
\seats{-1.5}{-8.55}
\seats{-1.5}{-9.00}
\node at (-1.2, -9.38) {$\cdots$};
\seats{-1.5}{-9.75}

%% ── Student nodes: group A ───────────────────────────────────────
\node[studentnode, plasmaA, text=plasmaA] (a1) at (3.5,  0.45) {$a_1$};
\node[studentnode, plasmaA, text=plasmaA] (a2) at (3.5, -0.45) {$a_2$};
\node at (3.5, -1.1) {$\vdots$};
\node[studentnode, plasmaA, text=plasmaA] (ai) at (3.5, -2) {$a_i$};

%% ── Student nodes: group B ───────────────────────────────────────
\node[studentnode, plasmaB, text=plasmaB] (b1) at (3.5, -3.1) {$b_1$};
\node[studentnode, plasmaB, text=plasmaB] (b2) at (3.5, -4) {$b_2$};
\node at (3.5, -4.65) {$\vdots$};
\node[studentnode, plasmaB, text=plasmaB] (bj) at (3.5, -5.65) {$b_j$};

%% ── Student nodes: group C ───────────────────────────────────────
\node[studentnode, plasmaC, text=plasmaC] (cc1) at (3.5, -6.85) {$c_1$};
\node[studentnode, plasmaC, text=plasmaC] (cc2) at (3.5, -7.75) {$c_2$};
\node at (3.5, -8.45) {$\vdots$};
\node[studentnode, plasmaC, text=plasmaC] (ck) at (3.5, -9.35) {$c_k$};

%% ── Curly braces for groups ──────────────────────────────────────
\draw[-, decorate, decoration={brace, amplitude=6pt}, plasmaA]
  (4.2, 0.85) -- (4.2, -2.5) node[midway, right=8pt, plasmaA] {$A$};
\draw[-, decorate, decoration={brace, amplitude=6pt}, plasmaB]
  (4.2, -2.8) -- (4.2, -6.15) node[midway, right=8pt, plasmaB] {$B$};
\draw[-, decorate, decoration={brace, amplitude=6pt}, plasmaC]
  (4.2, -6.45) -- (4.2, -9.8) node[midway, right=8pt, plasmaC] {$C$};

%% ── Arrow from a1 to preference box ─────────────────────────────
\draw[-, dotted] (a1.east) -- (prefbox.west);
\node[above, yshift=10pt, xshift=15pt, font=\small] at ($(prefbox.west)$) {$P_s(a_1)$};

%% ── Crossing edges (seats -> students) ──────────────────────────
\draw[-] (-1.8,  0.4) -- (a1);
\draw[-] (-1.8,  0.00) -- (a2);
\draw[-] (-1.45,  0.45) -- (b1);
\draw[-] (-1.45, -2.35) -- (b2);
\draw[-] (-1.8, -5.75) -- (ai);
\draw[-] (-1.1, -5.75) -- (cc1);
\draw[-] (-1.1, -8.55) -- (cc2);
\draw[-] (-1.45, -8.55) -- (bj);
\draw[-] (-1.1, -9.00) -- (ck);

\end{tikzpicture}\caption{Graph illustrating an F1M.} \label{fig:model}
\end{figure}
In a graph representing the HUM-assignment problem, the vertices are comprised
of students and seats that belong to specific class sections, and an edge is
drawn between a student and a seat if
the corresponding class is on the student's preference list. We can formally define this
representation.

\begin{definition}
  Let $G = (V, E)$ be a bipartite graph where:
  \begin{itemize}
    \item
          $V = R \cup S$
    \item
          $E = \{(r, s_c) \mid r \in R, s_c \in S, c \in C, c \succ_r
            \emptyset\}$
  \end{itemize}
\end{definition}

In this form, the graph only represents our problem, not a solution. For a graph
to represent a solution or a matching, each student would only have one edge
connecting them to a single seat in a class in a way that adheres to class capacities. So,
we need a way to narrow down which edges lead to a viable solution. That means
we need a way to judge edges against each other: we need to assign value to
them. To accomplish this, we can give each edge a weight that represents how
conducive it is to the constraints of our problem based on how well that match
would accomplish our goals. Recall that we want to satisfy two goals: every
student is assigned to a class they prefer, and all reserved seats are filled by
students of that reserved group. We
call a matching \textit{full} if every student is matched to a seat they prefer. 
Because we these two goals are equally important to us, we can simply increment
the weight of each edge by 1 for each goal it satisfies. We can now supplement
our previous definition of a graph.

\begin{definition}
  \label{weighted-def}
  Let $G = (V, E)$ be a bipartite graph where:
  \begin{itemize}
    \item
          $V = S \cup C$
    \item
          $E = \{(r, s_c) \mid r \in R, s_c \in S, c \in C, c \succ_r
            \emptyset\}$
    \item
          $w : E \rightarrow \{1 , 2 \} $ is a weight function
          defined as \[ w(r, s_c) = \mathbf{1}\{\text{if student $r$ prefers class
          $c$}\} + \mathbf{1}\{\text{$s_c^T$ is reserved for $r \in T$}\} \]
  \end{itemize}
\end{definition}

\begin{figure}[H] \centering
\definecolor{plasmaA}{HTML}{0D0887}
\definecolor{plasmaB}{HTML}{CC4678}
\definecolor{plasmaC}{HTML}{F89441}
  \begin{tikzpicture}[
  classnode/.style={circle, draw, minimum size=1cm, inner sep=0},
  studentnode/.style={circle, draw, minimum size=0.8cm, inner sep=0, thick},
  weight/.style={midway, fill=white, inner sep=1pt, font=\small},
  ->, >=Stealth
]

%% ── Column headers ──────────────────────────────────────────────
\node at (-3, 1.8) {$\mathcal{C}$};
\node at (-3, 1.2) {\textsc{classes}};
\node at (-1.25, 1.8) {$\mathcal{S}$};
\node at (-1.25, 1.2) {\textsc{seats}};
\node at (3.5,  1.8) {$R$};
\node at (3.5,  1.2) {\textsc{students}};

%% ── Preference box (top right) ──────────────────────────────────
\node[draw, minimum width=2.8cm, minimum height=0.7cm, align=center]
(prefbox) at (7.5, 0.2) {$c_1 \succ_{a_1} c_2 \succ_{a_1} c_n$};

%% ── Helper macro: 4 coloured squares in a row ───────────────────
\newcommand{\seats}[2]{
  \draw[plasmaA, thick] (#1-0.45, #2-0.15) rectangle (#1-0.15, #2+0.15);
  \draw[plasmaB, thick] (#1-0.10, #2-0.15) rectangle (#1+0.20, #2+0.15);
  \draw[plasmaC, thick] (#1+0.25, #2-0.15) rectangle (#1+0.55, #2+0.15);
  \draw[black,   thick] (#1+0.60, #2-0.15) rectangle (#1+0.90, #2+0.15);
}

%% ── Class C1 ────────────────────────────────────────────────────
\node[classnode] (c1) at (-3, 0) {$c_1$};
\seats{-1.5}{ 0.45}
\seats{-1.5}{ 0.00}
\node at (-1.2, -0.38) {$\cdots$};
\seats{-1.5}{-0.75}

%% ── Class C2 ────────────────────────────────────────────────────
\node[classnode] (c2) at (-3, -2.8) {$c_2$};
\seats{-1.5}{-2.35}
\seats{-1.5}{-2.80}
\node at (-1.2, -3.18) {$\cdots$};
\seats{-1.5}{-3.55}

%% ── Vertical dots between C2 and Cn-1 ──────────────────────────
\node at (-3, -4.5) {$\vdots$};

%% ── Class Cn-1 ──────────────────────────────────────────────────
\node[classnode] (cn1) at (-3, -6.2) {$c_{n-1}$};
\seats{-1.5}{-5.75}
\seats{-1.5}{-6.20}
\node at (-1.2, -6.58) {$\cdots$};
\seats{-1.5}{-6.95}

%% ── Class Cn ────────────────────────────────────────────────────
\node[classnode] (cn) at (-3, -9.0) {$c_n$};
\seats{-1.5}{-8.55}
\seats{-1.5}{-9.00}
\node at (-1.2, -9.38) {$\cdots$};
\seats{-1.5}{-9.75}

%% ── Student nodes: group A ───────────────────────────────────────
\node[studentnode, plasmaA, text=plasmaA] (a1) at (3.5,  0.45) {$a_1$};
\node[studentnode, plasmaA, text=plasmaA] (a2) at (3.5, -0.45) {$a_2$};
\node at (3.5, -1.1) {$\vdots$};
\node[studentnode, plasmaA, text=plasmaA] (ai) at (3.5, -2) {$a_i$};

%% ── Student nodes: group B ───────────────────────────────────────
\node[studentnode, plasmaB, text=plasmaB] (b1) at (3.5, -3.1) {$b_1$};
\node[studentnode, plasmaB, text=plasmaB] (b2) at (3.5, -4) {$b_2$};
\node at (3.5, -4.65) {$\vdots$};
\node[studentnode, plasmaB, text=plasmaB] (bj) at (3.5, -5.65) {$b_j$};

%% ── Student nodes: group C ───────────────────────────────────────
\node[studentnode, plasmaC, text=plasmaC] (cc1) at (3.5, -6.85) {$c_1$};
\node[studentnode, plasmaC, text=plasmaC] (cc2) at (3.5, -7.75) {$c_2$};
\node at (3.5, -8.45) {$\vdots$};
\node[studentnode, plasmaC, text=plasmaC] (ck) at (3.5, -9.35) {$c_k$};

%% ── Curly braces for groups ──────────────────────────────────────
\draw[-, decorate, decoration={brace, amplitude=6pt}, plasmaA]
  (4.2, 0.85) -- (4.2, -2.5) node[midway, right=8pt, plasmaA] {$A$};
\draw[-, decorate, decoration={brace, amplitude=6pt}, plasmaB]
  (4.2, -2.8) -- (4.2, -6.15) node[midway, right=8pt, plasmaB] {$B$};
\draw[-, decorate, decoration={brace, amplitude=6pt}, plasmaC]
  (4.2, -6.45) -- (4.2, -9.8) node[midway, right=8pt, plasmaC] {$C$};

%% ── Arrow from a1 to preference box ─────────────────────────────
\draw[-, dotted] (a1.east) -- (prefbox.west);
\node[above, yshift=10pt, xshift=15pt, font=\small] at ($(prefbox.west)$) {$P_s(a_1)$};

%% ── Crossing edges (seats -> students) ──────────────────────────
% From C1 seats area to students
\draw[-] (-1.8,  0.45) -- node[weight, above] {2}(a1);
\draw[-] (-0.75,  0) -- node[weight, below left] {1}(a1);
% From C2
\draw[-] (-1.8, -2.35) -- node[weight, above] {2}(a1);
\draw[-] (-0.75,  -2.35) -- node[weight, below left] {1}(a1);
% From Cn
\draw[-] (-1.8, -8.55) -- node[weight, above] {2}(a1);
\draw[-] (-0.75, -8.55) -- node[weight, below left] {1}(a1);

\end{tikzpicture}
\caption{Graph illustrating weighted edges for student $a_1$.} \label{fig:weighted-example}
\end{figure}
Now, we have a way to compare edges to each other to allow us to find a
solution if one exists. Based on the way we've set up our weights, we know that
the "best" available solution (which satisfies the most constraints) will be the
graph with the maximum possible weight when
only one edge remains for each student. Our max-weight matching then has the
properties required by our F1M (Definition \ref{def:f1m}), being that students of each group
are prioritized assignments to classes they prefer, as well as each
group being represented fairly within each class.

\begin{definition}
  A matching $\mu$ is a \emph{maximum-weight matching}
  if for all F1M matchings $\mu'$: \[ \sum_{r \in R} w(r, \mu(r)) \geq \sum_{r
      \in R} w(r, \mu'(r)) \]
\end{definition}

Observe that a maximum-weight matching would not only serve as a F1M
solution, but it would also (by nature of the way we assign weights based on
student preferences) ensure the solution is one in which the greatest number
of students are assigned to a class on their preference list. So, viewing the
problem through the lens of a weighted graph provides us with a helpful
framework for satisfying the problem.

\section{The Challenge of Stability} 
We've determined how to find a solution to the F1M problem from Definition
\ref{def:f1m}, but there is one more aspect to the problem we must consider.
Recall that matching is often studied under the analysis of
\textit{stability}. Notably, “stability” in our case adopts a different
meaning than in a traditional stable matching problem. Because we are
working with one-sided preferences, we do not consider “cheating” as a
violation of stability between students and classes, but rather between
students themselves. That is, we can consider a matching stable if there are
no blocking pairs \emph{between students}.

\begin{definition}
  \label{def:stability}
  A matching $\mu$ is \emph{stable} if there exists no \textit{blocking pair} between
  students
  $r_1, r_2 \in R$ such that $\mu(r_2) >_{r_1} \mu(r_1)$ and $\mu(r_1) >_{r_2}
    \mu(r_2)$.
\end{definition}

This notion of stability proves difficult for our problem, however. If we look
back at our original definition of the HUM-assignment problem (Definition
\ref{def:overview}) with class capacities and
incomplete preferences, we will find that in some instances it is possible there
may not even exist a stable matching to find.

\begin{theorem} \label{thm:unstable}
  There is not always a stable matching with quotas and
  incomplete preferences.
\end{theorem}

\newpage

\begin{proof}
  Let $C = \{ c_1, c_2\}$, where there are two groups of student
  $A$ and $B$ such that for each $c \in C, q_c^A, q_c^B = 1$ (each class can
  have at most one student of
  each group). Let $R = \{ a_1, b_1, a_2, b_2 \}$. Let the preference lists be as
  follows: \[
    \begin{array}{ll} a_1: & c_1           \\ b_1: & c_2 \\ a_2: & c_1 \succ c_2 \\
             b_2:      & c_2 \succ c_1
    \end{array}
  \]

  In this case, regardless of the order of matching or algorithm, there is no
  stable matching. The $A$ students will both wish to be matched to $c_1$, which
  only has one $A$ seat, and both $B$ students wish to be matched to $c_2$
  which only has one $B$ seat. So, when the first round of matching is done,
  that means that there is still an open $B$ seat for $c_1$ and an open
  $A$ seat for $c_2$, which the unhappy $A$ and $B$ students can then switch
  into on their own, respectively.
\end{proof}

Defining stability in this way is, then, too strict. Recall that even
determining whether there is a stable matching or not is \textit{NP-complete} in many
scenarios similar to this one \cite{santhini}. So, we need to relax our constraints
to allow us to explore some less restrictive notion of stability. For now, let’s
simplify the problem. At a basic level,
before we even think about stability, we want to work in an environment where we
can be sure an algorithm would respect all student preferences, such that no student
is assigned to a class they did not list as an option. 

To temporarily remove our notion of stability, we can alter our preferences from a
ranked system to what is known as \textit{0/1 preferences}. In 0/1 preferences,
students give a class a 1 if they are okay with being matched to it and a 0 if
it is not an option for them. This way,
there cannot be a notion of stability because all preferred classes have the
same rank. Now, for ease of notation, we
will let $S_{res}$ be the set of reserved seats across all classes. Observe that
this includes all seats other than the set of free seats, or formally: 
 \[
   |S_{res}| = \sum_{\substack{c \in C \\ t \neq F}} q_c^t
  \] 
Notably, this simplified environment still lends itself well to the same
weighted graph construction from Definition \ref{weighted-def}, so we can
continue to use a weighted graph to represent our problem.

\begin{theorem}
  \label{max-weight} A matching fills all reserved seats while only assigning students to classes they prefer if and only if it is the maximum-weight matching
  where $w(\mu) = |R| + |S_{res}|$.
\end{theorem}

\begin{proof}
  First we will prove the forward direction. Let $\mu$ be a matching that
  assigns every student to a seat they
  prefer while also filling reserved seats first, such that all reserved seats
  are filled before free seats. By construction of the weights, we can
  determine a lower and upper bound for $w(\mu)$. 


  \begin{enumerate}
    \item
          \textbf{Lower bound of $\mu$:} Each edge exists only
          if the student prefers that seat, so every edge in $\mu$ contributes at
          least 1. By our assumption of characteristics of $\mu$, every student in
          $\mu$ got a seat they prefer, so summing over all students contributes at
          least $|R|$ to $w(\mu)$. We also assumed each reserved seat was assigned to
          a student in $\mu$, which adds an extra +1 to the weight for the edge from
          each of those reserved seats. Summing over all reserved seats gives an
          additional contribution of $|S_{res}|$, because the rest of the seats are still
          1-weight edges. Therefore, we can bound $\mu$: \[ w(\mu) \geq |R| +
            |S_{res}|.
          \]

    \item
      \textbf{Upper bound of $\mu$:} From Definition \ref{weighted-def}, we know each edge
          weight is at most 2. Similarly to the lower bound, we know $\mu$ has $|R|$
          edges because each student was matched to one of their preferences.
          However, because only the reserved seats contribute an additional weight
          to an edge, we know that the number of 2-weight edges $\leq |S_{res}|$. So,
          the maximum summed weight of all edges must be $2|S_{res}| + |R| -
          |S_{res}|$,
          representing the maximum number of 2-weight edges plus all students
          assigned to an overflow seat. Note that is equivalent to $|R| +
          |S_{res}|$.
          Therefore, we can bound $\mu$: \[ w(\mu) \leq |R| + |S_{res}|. \]
  \end{enumerate}

  So, $\mu$ is a maximum-weight graph with a lower bound of $|R| + |S_{res}|$ and an
  upper bound of $|R| + |S_{res}|$, so it must be that a matching that fully respects
  student preferences and seat reservations has a weight $w(\mu) = |R| + |S_{res}|$.

  Now, we prove the backwards direction. Let $\mu$ be a maximum-weight matching
  where $w(\mu) = |R| + |S_{res}|$. The term $|R|$ implies each student was assigned
to a class. By our construction of $G$ such that an edge between a student and a
class only exists if the student prefers that class, the fact that each student
is assigned to a class must mean it is on their preference list. Additionally,
the $|S_{res}|$ term implies every reserved seat is assigned, because by our weight
function each edge only has additional weight $+1$ if the edge is between a
student of the same group as the seat. So, a maximum-weight matching where 
$w(\mu) = |R| + |S_{res}|$ is a matching that fills all reserved seats while only
assigning students to classes they prefer.
\end{proof}

Theorem \ref{max-weight} shows us that we can identify a way to 
assign each student to a class they prefer while respecting group fairness
constraints if such a matching exists. From here, we can begin to reintroduce
stability. While it may not be feasible to find stability between every student,
perhaps we can start by comparing just students of the same group.  We are
introducing a new notion of stability here, where we will consider a matching
stable if it exhibits \textit{intra-group stability}. This
means that for a student of any group, that student does not form a blocking pair
with another student \textit{of the same group}.

\begin{definition} \label{def:intra}
  A matching $\mu$ is \emph{intra-group stable} if, for any two students $t_1$
  and $t_2$ for all
  groups $T \subset R$, $\mu(t_1) >_{t_1} \mu(t_2)$  or $\mu(t_2) >_{t_2}
    \mu(t_1)$ such that there exist no intra-group blocking pairs.
\end{definition}

With our new goal of intra-group stability, we can now add to our
understanding of a viable solution to our HUM-assignment problem.

\begin{definition} \label{def:ispim}
  An \emph{intra-group stable, group-fair 1-sided matching (ISF1M)} is a function $\mu :
    R \rightarrow S$ such that
  \begin{itemize}
    \item
          Each student is assigned to a class they prefer (the matching is full): \\
          $\mu(r) \succ_r \emptyset \text{ for all }r \in
            R$
    \item
          No class exceeds its capacity:
         $|\{r \in R : \mu(r) = s_c\}| \leq q_c$ \text{ for all } $s_c \in c \subset S$
    \item
          No class exceeds its capacity for each student group: \\ 
          $|\{t \in T : \mu(t) = s_c^T\}| \leq q_c^T + q_c^F$
    \item
          There exist no intra-group blocking pairs: \\ for all $t_1, t_2 \in T \subset R$, 
          $\mu(t_1) >_{t_1} \mu(t_2)$ or $\mu(t_2) >_{t_2} \mu(t_1)$
  \end{itemize}
\end{definition}

For now, our goal will be to develop an algorithm that finds an ISF1M to solve
our problem. We will classify our previous definition of stability (Definition
\ref{def:stability}) as \textit{inter-group stability} (blocking pairs involving
students of different groups) to draw a distinction between the two notions.
For now, we will consider an ISF1M to be a successful solution to the
HUM-assignment problem even though it is not inter-group stable. Though we would
ideally guarantee stability between all students, looking at the
problem through the lens of intra-group stability allows us to give some
theoretical guarantee about minimizing the amount of swapping that will occur
in an assignment while still remaining computationally feasible, as opposed to turning
to less-reliable heuristic solutions in an attempt to solve the broader conception of the problem.

